services:
  next-app:
    build: 
      context: ./frontend
      dockerfile: DockerFile
    ports:
      - 8080:8080
  express-app:
    build: 
      context: ./backend
      dockerfile: DockerFile
    ports:
      - 3001:3001